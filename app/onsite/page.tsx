import OnsiteViewer from '@/components/OnsiteViewer';

export const metadata = {
  title: 'On-site Campaigns — Campaign Intelligence Dashboard',
  description: 'MoEngage Web SDK live preview and debug panel for On-site (OSM) campaigns',
};

export default function OnsitePage() {
  return <OnsiteViewer />;
}
