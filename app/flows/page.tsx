import FlowsList from '@/components/FlowsList';

export const metadata = {
  title: 'Active Flows — Campaign Intelligence Dashboard',
    description: 'MoEngage active journey flows and automation overview',
    };

    export default function FlowsPage() {
      return <FlowsList />;
      }